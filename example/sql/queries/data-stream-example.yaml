---
name: data-stream-example
description: "An example for Flink DataStream API."
target-table: "data_stream_example_output"
code: >
    from pyflink.common.typeinfo import Types

    execution_output = stream_env.from_collection(
        collection=[(1, 'aaa'), (2, 'bb'), (3, 'cccc')],
        type_info=Types.ROW_NAMED(['id', 'name'], [Types.INT(), Types.STRING()])
    )
flinkProperties: {}
